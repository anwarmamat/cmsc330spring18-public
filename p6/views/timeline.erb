<section>
  <form method="post" action="">
  <input type="hidden" value="<%= token %>" name="token">
  <fieldset>
  <div>
    <input type="text" placeholder="Say something clever" name="content">
  </div>
  <div>
    <input type="submit" value="Submit">
  </div>
  </fieldset>
  </form>
</section>

<section>
  <% epsilons.each do |epsilon| %>
    <div class="box">
      <img class="box-avatar" src="/avatars/<%= epsilon[:avatar] %>" />
      <div class="box-body">
        <a href="/user?q=<%= epsilon[:user] %>"><%= epsilon[:user] %></a> ◇ <%= epsilon[:date] %>
        <p> <%= epsilon[:content] %> </p>
      </div>
    </div>
  <% end %>
</section>
